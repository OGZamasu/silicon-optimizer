# Laya, in <library>/Engine Cache/laya-env
# The versions installed and in use on the maintainer's Mac on 2026-09-23 (Python 3.13).
# The locks hold to them, so re-running an installer over a working environment changes
# nothing, and a new environment gets the combination that is known to work.
# Packages installed from a URL or a commit are pinned in the lock script, not here.
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
filelock==4.0.1
fsspec==2026.9.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface-hub==1.32.0
idna==3.20
laya-mlx==0.1.0
mlx==0.32.2
mlx-metal==0.32.2
numpy==2.5.3
packaging==26.3
pyyaml==6.0.3
tokenizers==0.23.2
tqdm==4.70.1
typing-extensions==4.16.0
